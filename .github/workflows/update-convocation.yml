name: Aggiorna convocazione Discord

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts
      - run: npm run update
        env:
          DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
          CONVOCATION_CHANNEL_ID: "1467875055525429360"
          CONVOCATION_MESSAGE_ID: "1543278162195128322"
          CONVOCATION_ROLE_ID: "1466909921554464825"
          CONVOCATION_ENTRANTS: "47"
          CONVOCATION_TRACK: "TEXAS"
          CONVOCATION_DATE: "21/09/2026"
          CONVOCATION_TIME: "21:30"
          CONVOCATION_WEATHER: "Clear"
          CONVOCATION_LAPS: "120"
          CONVOCATION_TYRES: "5"
          CONVOCATION_LOGO_URL: "https://test.i-cin.it/images/nextrace/loghi/truck/texas.png"
